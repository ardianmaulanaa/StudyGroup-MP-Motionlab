## 🧭 Langkah-Langkah yang Sudah Dilakukan

1️⃣ **Membuat Repository di GitHub**  
- Membuat repository baru bernama `StudyGroup-MP-Motionlab`.  
- Tujuan: tempat penyimpanan kode dan file tugas secara online.  

2️⃣ **Clone Repository ke Lokal**  
```
git clone https://github.com/ardianmaulanaa/StudyGroup-MP-Motionlab.git
```  
Tujuan: menyalin (download) repository dari GitHub ke komputer lokal.  

3️⃣ **Masuk ke Folder Project**  
```
cd StudyGroup-MP-Motionlab
```  
Tujuan: berpindah ke folder project agar perintah Git berjalan di repo ini.  

4️⃣ **Inisialisasi Repository (Jika Belum Ada)**  
```
git init
```  
Tujuan: menjadikan folder ini sebagai repository Git lokal.  

5️⃣ **Mengecek Status**  
```
git status
```  
Tujuan: melihat apakah ada file baru, diubah, atau siap di-commit.  

6️⃣ **Membuat Branch Baru**  
```
git branch sg1-Mobile-Programming
git checkout -b sg1-Mobile-Programming
```  
Tujuan: membuat cabang (branch) baru agar pekerjaan tidak memengaruhi branch utama (`main`).  

7️⃣ **Mengganti Nama Branch**  
```
git branch -m minggu-ke-1-Pengenalan-Git
```  
Tujuan: mengganti nama branch agar sesuai format tugas minggu ke-1.  

8️⃣ **Menambahkan File ke Staging Area**  
```
git add README.md
git add Rangkuman-GIT.md
```  
Tujuan: menyiapkan file agar siap di-commit.  

9️⃣ **Membuat Commit**  
```
git commit -m "Tambah README.md"
```  
Tujuan: menyimpan perubahan secara permanen di repository lokal.  

🔟 **Menghubungkan Repository Lokal ke GitHub**  
```
git remote add origin https://github.com/ardianmaulanaa/StudyGroup-MP-Motionlab.git
git remote -v
```  
Tujuan: menghubungkan repository lokal dengan repository GitHub.  

1️⃣1️⃣ **Mengirim (Push) ke GitHub**  
```
git push -u origin minggu-ke-1-Pengenalan-Git
```  
Tujuan: mengirim branch dan commit dari lokal ke GitHub.  

1️⃣2️⃣ **Mengecek Branch di GitHub**  
- Mengecek branch `minggu-ke-1-Pengenalan-Git` sudah muncul.  
- File `README.md` dan `Rangkuman-GIT.txt` berhasil terunggah.  

1️⃣3️⃣ **Menghapus File yang Tidak Diperlukan**  
```
git rm Rangkuman-GIT.txt
git commit -m "Hapus file txt"
git push
```  
Tujuan: menghapus file `.txt` dari repository GitHub.  

1️⃣4️⃣ **Menambahkan File Baru**  
- Membuat file baru `Rangkuman-GIT.md` di VS Code.  
- Mengisi dengan isi rangkuman perintah Git lengkap.  
- Lalu menjalankan:  
```
git add Rangkuman-GIT.md
git commit -m "Tambah file rangkuman Git lengkap"
git push
```  
Tujuan: menambahkan file Markdown berisi catatan ringkas Git.  

1️⃣5️⃣ **Mengecek Hasil di GitHub**  
- Branch `minggu-ke-1-Pengenalan-Git` berisi dua file:  
  - `README.md`  
  - `Rangkuman-GIT.md`  
- Semua langkah sudah sesuai instruksi tugas.  
